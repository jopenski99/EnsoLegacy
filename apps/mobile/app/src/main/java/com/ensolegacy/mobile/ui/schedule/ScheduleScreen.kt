package com.ensolegacy.mobile.ui.schedule

import androidx.compose.runtime.Composable
import com.ensolegacy.mobile.ui.common.PlaceholderScreen

/**
 * Schedule tab — care reminders and seasonal tasks (rain checks, watering,
 * repotting windows). Built in a later phase; placeholder for now.
 */
@Composable
fun ScheduleScreen() {
    PlaceholderScreen(
        title = "Schedule",
        note = "Care reminders and seasonal tasks for your trees will live here.",
    )
}
