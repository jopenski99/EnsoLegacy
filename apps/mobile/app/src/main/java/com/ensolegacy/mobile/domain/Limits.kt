package com.ensolegacy.mobile.domain

/**
 * Product limits mirrored from `@enso/shared` (packages/shared/src/constants.ts).
 * Keep these values identical across web and mobile.
 */

/** Max photos attached to a single milestone (the "10-photo flow"). */
const val MAX_MILESTONE_PHOTOS = 10
