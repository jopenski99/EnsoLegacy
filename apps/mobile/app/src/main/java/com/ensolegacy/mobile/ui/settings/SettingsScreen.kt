package com.ensolegacy.mobile.ui.settings

import androidx.compose.runtime.Composable
import com.ensolegacy.mobile.ui.common.PlaceholderScreen

/**
 * Settings tab — account, backup/sync, export, and preferences. Built in a
 * later phase; placeholder for now.
 */
@Composable
fun SettingsScreen() {
    PlaceholderScreen(
        title = "Settings",
        note = "Account, backup, and Tree Passport export will live here.",
    )
}
